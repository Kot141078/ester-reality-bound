# Cross-Repo Pointers — AGL v0.1
## Short canonical bridge texts for repository integration

**Package:** Actor Grounding Layer v0.1  
**Short name:** AGL v0.1  
**Canonical home:** `ester-reality-bound`  
**Author:** Ivan Kotov  
**Location:** Brussels  
**Year:** 2026  

---

## 1. Purpose

This file provides short, reusable pointer texts for integrating the AGL package
into adjacent repositories without duplicating the canonical content.

The goal is simple:
- one canonical home,
- multiple short references,
- no cross-repo ambiguity,
- and no duplicate “mini-AGL” fragments that drift over time.

---

## 2. Canonical rule

**AGL v0.1 lives canonically in `ester-reality-bound`.**

Other repositories may:
- point to it,
- contextualize it,
- mention its role,
- and bridge it to adjacent layers,

but they MUST NOT become competing homes of the grounding layer.

---

## 3. Pointer for `advanced-global-intelligence`

### 3.1 Short form
AGL v0.1 defines the upstream grounding layer that determines whether a signal, actor, node, or perceptual path is sufficiently grounded in real present execution state before runtime reliance or action progression is allowed.

### 3.2 Longer form
As the AGI stack moves from abstract capability toward long-lived, bounded coexistence, it becomes insufficient to ask only whether a system can reason or act. A prior question appears: whether the source initiating action is sufficiently grounded in present reality to support runtime reliance at all. AGL v0.1 addresses this upstream boundary. Its canonical home is `ester-reality-bound`.

---

## 4. Pointer for `sovereign-entity-recursion`

### 4.1 Short form
AGL v0.1 is the upstream grounding layer that sits before ARL dispute review: it determines whether a signal or actor may lawfully support runtime reliance before standing, admissibility, freeze, or review are allowed to proceed.

### 4.2 Longer form
ARL governs procedural conflict once a dispute has entered review. AGL sits one layer earlier. It qualifies whether the initiating actor, signal, node, or perceptual path is grounded enough in present execution state to lawfully support runtime reliance in the first place. This keeps procedural review from being built on already abstracted initiation. The canonical home of AGL is `ester-reality-bound`.

---

## 5. Pointer for `ester-clean-code`

### 5.1 Short form
AGL v0.1 is not implemented here as canonical doctrine. `ester-clean-code` should only carry implementation-facing bridges and runtime hooks derived from the canonical AGL layer.

### 5.2 Longer form
The grounding layer belongs canonically to the public L4 / reality-bound repository, not to the executable codebase. `ester-clean-code` may later carry implementation-facing bridges, hook points, state transitions, gate logic, and witness/event bindings related to AGL, but the codebase must not become a parallel normative source.

---

## 6. One-line public formulation

AGL v0.1 defines the upstream grounding layer that determines whether a signal, actor, node, or perceptual path is sufficiently grounded in real present execution state before runtime reliance or action progression is allowed.

---

## 7. Explicit bridge

**L4 Hardware Perimeter ↔ Actor Grounding Layer ↔ ARL / runtime reliance**

---

## 8. Hidden bridges

- pre-admissibility / standing discipline  
- L4 Witness traceability without substituting for grounding

---

## 9. Earth paragraph

On a real warehouse floor, a dispute review may be perfectly procedural and still start from the wrong source: a tired operator, a stale sensor, a spoofed badge, or a relay that is already out of the real circuit. Short references exist to remind adjacent repositories of this fact. The rulebook for that earlier grounding question lives in one place, not in scattered fragments.

--------------------------------
